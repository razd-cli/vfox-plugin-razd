name: Test mise Integration

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  test-mise:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Cache mise
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
          key: mise-${{ runner.os }}-v1
      
      - name: Install mise
        run: |
          curl https://mise.run | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH
      
      - name: Verify mise installation
        run: mise --version
      
      - name: Install plugin from source
        run: |
          mise plugin install --yes razd "file://$GITHUB_WORKSPACE"
      
      - name: Verify plugin installation
        run: mise plugin list | grep razd
      
      - name: List available versions
        run: mise ls-remote razd
      
      - name: Install razd latest
        run: mise install razd@latest
      
      - name: Activate razd
        run: mise use razd@latest
      
      - name: Verify razd command
        run: |
          eval "$(mise activate bash)"
          razd --version
